# Changelog

All notable changes to the MCP Builder (Lite) plugin are documented here.
This project adheres to [Semantic Versioning](https://semver.org/).

## [1.0.0] - 2026-06-23

### Added
- Initial free release.
- **1 agent** — `mcp-architect` (trimmed), orchestrates scaffolding and first-tool design.
- **2 skills** — `scaffold-mcp-server` and `design-mcp-tool`.
- README (with MCP source citations), free-use license, and `.gitignore`.
- Content written against the current MCP specification and the official
  TypeScript (`@modelcontextprotocol/sdk`) and Python (`mcp` / FastMCP) SDKs.
